[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — every route serves index.html so client-side state
# (open tabs, etc.) doesn't 404 on refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
